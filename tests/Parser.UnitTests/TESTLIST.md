# Список тестов

## Класс Parser

## Разбор арифмитических операций
- [x] Разбор целочисленных литералов: `123`
- [x] Разбор вещественных литералов: `465.789`
- [x] Разбор остатка от деления: `5 % 3, 19 % 7 % 2`
- [x] Деление на ноль и остаток отделения от нуля: `10 / 0, 15 % 0`
- [x] Разбор унарного плюса: `+5, +3.14`
- [x] Разбор унарного минуса: `-10, -2.5`
- [x] Правая ассоциативность унарного плюса и минуса: `2 * +3, 2 * -3`
- [x] Правая ассоциативность степени: `2 ^ 3 ^ 2`
- [x] Приоритет умножения над сложением: `2 + 3 * 4`
- [x] Приоритет деления над вычитанием: `10 - 8 / 2`
- [x] Приоритет степени над умножением: `2 * 3 ^ 2`
- [x] Группировка скобками: `(2 + 3) * 4`
- [x] Возведение в степень: `(-1) ** 2, -1 ** 2`
- [x] Комбинация степени с унарными операторами: `-2 ^ 3`

## Работа с переменными
- [x] Объявление переменной без инициализации: `let x: int; var y;`
- [x] Объявление переменной и константы с одним и тем же идентификатором: `let x: int; var x;`
- [x] Объявление одной изменяемой переменной: `var x = 2 + 2; var y = x + 5;`
- [x] Объявление нескольких изменяемых переменных: `var x = 1; var y = 2;var z = 3;x + y * z;`
- [x] Разбор выражений, разделённых точками с запятой: `1 + 2; 2 * 5; 4.5;`
- [x] Объявление константы: `let pi: float = 3.14159; 4.0 * pi * 4.0;`
- [x] Объявленные переменные используются в сложном выражении: `var a = 10; var b = 2; a = 5; b = a + 1;`

## Разбор битовых операций 
- [x] Разбор логического AND: `5 & 3, 7 & 2, 0 & 5`  
- [x] Разбор логического OR: `5 | 3, 4 | 1`  
- [x] Разбор логического XOR: `5 ^ 3, 7 ^ 2, 0 ^ 0`  
- [x] Разбор логического NOT: `~0, ~1, ~5`  
- [x] Разбор комплексного выражения: `~2 | 1, ~0 | 1, ~5 & 3, ~4 ^ 1`  

## Разбор встроенных функций
- [x] Разбор функции: `abs(-10), abs(-7.5)`
- [x] Разбор функции: `min(10, 4, 1, 56), min(-1, -50, 0)`
- [x] Разбор функции: `max(1, 2, 3, 4, 5), max(-10, -8, -1)`
- [x] Разбор функции: `pow(-1, 2), pow(1, 2), pow(2, 3), pow(-2, 3)`
- [x] Разбор функции: `round(3.5), "round(3.7), round(-3.2), round(-3.5), round(-3.7)`
- [x] Разбор функции: `ceil(3.2), ceil(3.0), ceil(-3.2), ceil(-3.0)`
- [x] Разбор функции: `floor(3.8), floor(3.0), floor(-3.8), floor(-3.0)`

## Ошибки
- [x] Незакрытая скобка: `(1 + 2`
- [x] Лишняя скобка: `1 + 2)`
- [x] Неполное выражение: `1 +`
- [x] Пустая функция: `max()`
- [x] Лишняя запятая: `max(1, 2,)`
- [x] Отсутсвие запятой: `max(1 2)`
- [x] Неправильный формат числа: `.25`

## Негативные сценарии
- [x] Использование переменных без объявления: `x + 1;`
- [x] Объявление переменной с пустым идентификатором: `var = 1; x + 2;`
- [x] Объявление переменной, которая уже сущетсвует: `let x: int = 1; let x: int = 10;`, `var x = 5; var x = 6;`
- [x] Отсутсвие типа у константы: `let x = 1;`


## Разбор цикла for
### Позитивные сценарии
- [x] Разбор цикла: `for i = 0, i < 10, i = i + 1 in { }`
- [x] Разбор вложенного цикла: `for i = 0, i < 3, i = i + 1 in { for j = 0, j < 2, j = j + 1 in {} }`
- [x] Разбор цикла в обратном порядке: `for i = 3, i != 0, i = i - 1 in {}`
- [x] Разбор цикла с break: `for i = 0, i < 5, i = i + 1 in { if i == 2 { break; } }`
- [x] Разбор цикла с continue: `for i = 0, i < 5, i = i + 1 in { if i == 2 { continue; } }`
- [x] Разбор бесконечно цикла: `for , , in {}`
### Негативные сценарии
- [x] Разбор цикла с break вне цикла: `for i = 0, i < 5, i = i + 1 in {} break;`
- [x] Разбор цикла с continue вне цикла: `for i = 0, i < 5, i = i + 1 in {} continue;`

## Ращбор кастомных функций
### Позитивные сценарии
- [x] Разбор функции без параметров: `func foo(): int { return 20; }`
- [x] Разбор функции с 1 параметров: `func inc(x: int): int { return x + 1; }`
- [x] Разбор функции с несколькими параметрами: `func foo(a: int, b: int): int { return a + b; }`
- [x] Разбор функции функции с рекурсией: `func foo(x: int): int { if x == 5 then { return x; } return foo(x - 1); }`
- [x] Разбор функции с возвратом разных типов: `func foo(): int { return 0; }, func foo(): float { return 1.5; }, func foo(): bool { return true; } func foo(): string { return "abc"; }`

## Разбор цикла while
### Позитивные сценарии
- [x] Разбор цикла: `var i = 0; while (i < 2) { i = i + 1; }`
- [x] Разбор цикла с break: `var i = 0; while ( i < 5) { if i == 3 then { break; } i = i + 1; }`
- [x] Разбор цикла с continue: `while (i < 5) { if i == 3 then { continue; } i = i + 1; }`
### Негативные сценарии
- [x] Разбор цикла с break вне цикла: `while (i < 2) {} break;`
- [x] Разбор цикла с continue вне цикла: `while (i < 2 ) {} continue;`
 
## Разбор цикла do while
### Позитивные сценарии
- [x] Разбор цикла: `do { var i = 0; i = i + 1; } while( i < 5 )`
- [x] Разбор цикла с break: `do { var i = 0;  i = i + 1; } while( i < 5 ) `
- [x] Разбор цикла c continue: `do { var i = 0; if i == 1 then { continue; } i = i + 1;} while(i < 3)`
- [x] Разбор цикла с выполненной одной иттерацией: `do { var x = 5; } while(false)`
### Негативные сценарии
- [x] Разбор цикла с break вне цикла: `var x = 5; do { var a = x + 5; print(a); } while (x != 5); break;`
- [x] Разбор цикла c continue вне цикла: `var x = 5; do { var a = x + 5; print(a); } while (x != 5); continue;` 

## Разбор if else
### Позитивные сценарии
- [x] Разбор условия: `var x = 0; if x == 0 then { x = 5; }`
- [x] Разбор условия с else: `var x = 2; if x < 2 then { x = x + 1; } else { x = x - 1; }`
### Негативные сценарии
- [x] Ращбор условия, отсутсвуют фигурный скобки



