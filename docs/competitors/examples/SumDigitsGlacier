module Main

func main() {
    let n: int = input();
    let sum: int = 0;

    while (n > 0) {
        let digit: int = n % 10;
        sum = sum + digit;
        n = n // 10;
    }

    print(sum);
}